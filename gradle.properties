﻿org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
# Compile baseline stays on 1.20.1; runtime registry virtualization also rewrites
# payload-style registry facades introduced in 1.20.5+.
minecraftVersion=1.20.1
forgeVersion=47.2.0
loom.platform=forge
intermedVersion=8.0.0-alpha.2
intermedBuildId=alpha-2
